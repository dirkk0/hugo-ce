<?php
// ── Template for _private/secrets.php ──────────────────────────────────
// Copy to secrets.php in this folder and fill in the real value:
//
//   cp static/_private/secrets.php.example static/_private/secrets.php
//
// secrets.php is gitignored and .htaccess-denied. This template is committed,
// so keep a placeholder here, never the real value. Without secrets.php the
// dashboard locks itself out (blank password); the counter needs no secret.

define('STATS_PASS', '');   // /w/v1/stats.php dashboard, user in stats.php
